import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ShopFlow app smoke test', (WidgetTester tester) async {
    // Basic smoke test to verify the project compiles
    expect(1 + 1, equals(2));
  });
}
